{"canonical": "What causes memory fragmentation in long-running Python processes?", "paraphrases": ["Why do long-lived Python services suffer from heap fragmentation?", "memory fragmentation in long running python processes causes"], "traps": ["How do I profile memory usage in Python?"]}
{"canonical": "Why is our checkout API returning 504 on large carts?", "paraphrases": ["checkout API returns 504 errors on large carts why", "Why does the checkout API return a 504 for large carts?"], "traps": ["Why is our checkout API returning 401 on large carts?"]}
{"canonical": "How do I add a database index in Postgres without locking the table?", "paraphrases": ["add a database index in Postgres without locking the table", "How can I add an index in Postgres without table locking?"], "traps": ["How do I drop a database index in Postgres without locking the table?"]}
{"canonical": "What is the difference between asyncio and threading in Python?", "paraphrases": ["difference between asyncio and threading in Python", "In Python what is the difference between threading and asyncio?"], "traps": ["What is the difference between asyncio and multiprocessing in Python?"]}
{"canonical": "Explain the Python GIL in one paragraph", "paraphrases": ["explain the python GIL in a single paragraph", "In one paragraph explain the Python GIL"], "traps": ["Explain the Python garbage collector in one paragraph"]}
{"canonical": "How do I configure connection pooling in SQLAlchemy?", "paraphrases": ["configure connection pooling in SQLAlchemy how", "How is connection pooling configured in SQLAlchemy?"], "traps": ["How do I configure connection pooling in Django?"]}
{"canonical": "Why does my Docker build invalidate the cache on every run?", "paraphrases": ["Docker build invalidates the cache on every run why", "why does the docker build cache get invalidated every run"], "traps": ["Why does my Docker container exit immediately on every run?"]}
{"canonical": "What is the best way to handle retries with exponential backoff?", "paraphrases": ["best way to handle retries with exponential backoff", "How should retries with exponential backoff be handled?"], "traps": ["What is the best way to handle timeouts with exponential backoff?"]}
{"canonical": "How do I reduce cold start latency in AWS Lambda?", "paraphrases": ["reduce cold start latency in AWS Lambda how", "How can AWS Lambda cold start latency be reduced?"], "traps": ["How do I reduce memory usage in AWS Lambda?"]}
{"canonical": "What are the tradeoffs between REST and GraphQL for a public API?", "paraphrases": ["tradeoffs between REST and GraphQL for a public API", "For a public API what are the tradeoffs of GraphQL versus REST?"], "traps": ["What are the tradeoffs between REST and gRPC for a public API?"]}
{"canonical": "How does Redis handle key eviction when memory is full?", "paraphrases": ["how does Redis handle key eviction when memory is full", "When memory is full how does Redis evict keys?"], "traps": ["How does Redis handle key expiration when memory is full?"]}
{"canonical": "Why is my Postgres query doing a sequential scan instead of using the index?", "paraphrases": ["Postgres query does a sequential scan instead of using the index why", "why does postgres do a sequential scan rather than using my index"], "traps": [], "hard_traps": ["Why is my Postgres query using the index instead of a sequential scan?"]}
